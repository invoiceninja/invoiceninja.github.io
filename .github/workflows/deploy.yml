name: Deploy to GitHub Pages

on:
  push:
    branches:
      - v5-rework
  pull_request:
    branches:
      - v5-rework

permissions:
  contents: write

jobs:
  build-deploy:
    name: Build & Deploy
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          lfs: true
          fetch-depth: 0

      - name: Snapshot current live site for rollback
        if: github.event_name == 'push'
        run: |
          git tag -f pre-deploy-backup origin/master || true
          git push origin pre-deploy-backup --force || true

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Checkout Invoice Ninja UI icons source
        uses: actions/checkout@v4
        with:
          repository: invoiceninja/ui
          path: external/invoiceninja-ui
          fetch-depth: 1

      - name: Cache Docusaurus
        uses: actions/cache@v4
        with:
          path: |
            .docusaurus
            node_modules/.cache
          key: docusaurus-${{ runner.os }}-${{ hashFiles('package-lock.json') }}

      - name: Verify generated icons are up to date
        run: npm run icons:check

      - name: Generate OpenAPI docs
        run: npx docusaurus gen-api-docs invoiceninja

      - name: Build site
        run: npm run build
        env:
          NODE_OPTIONS: "--max-old-space-size=16384"

      - name: Deploy to GitHub Pages
        if: github.event_name == 'push'
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./build
          publish_branch: master
          force_orphan: true
